import { Routes } from '@angular/router';
import { MachineListComponent } from './machine-list.component';

export const MACHINE_LIST_ROUTES: Routes = [
  {
    path: '',
    component: MachineListComponent,
  },
];